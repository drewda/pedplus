- content_for :head do
  script
    | function injectProjects() {
    == "masterRouter.projects.reset(#{current_user.projects.to_json(:methods => [:geographic_center])});"
    | }

- content_for :body do
  .container-fluid
    #activity-box
      ul.tabs
        li.dropdown(data-dropdown="dropdown")
          a.dropdown-toggle = current_user.full_name
          ul.dropdown-menu
            li
              a Settings
            li.divider
            li
              = link_to "Sign Out", destroy_user_session_path, :method => :delete
        li 
          a(href="#message/new") M
    #top-bar
    #map-area
    #spinner

= render :template => '/app/layout'