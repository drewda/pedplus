<ul class="nav nav-pills">
    <li id="project-pill"><a href="#project/<%= projectId %>">Project</a></li>
    <li id="map-pill"><a href="#project/<%= projectId %>/map">Map</a></li>
    <% if (projectKind == 'pedplus') { %>
    <li id="model-pill"><a href="#project/<%= projectId %>/model">Model</a></li>
    <% } %>
    <li id="measure-pill"><a href="#project/<%= projectId %>/measure">Measure</a></li>
    <% if (projectKind == 'pedplus') { %>
    <li id="opportunity-pill"><a href="#project/<%= projectId %>/opportunity">Opportunity</a></li>
    <li id="design-pill"><a href="#project/<%= projectId %>/design">Design</a></li>
    <% } %>
    <li id="account-pill" class="dropdown">
    	<a class="dropdown-toggle" data-toggle="dropdown">
    		Account
    		<b class="caret"></b>
    	</a>
    	<ul class="dropdown-menu">
    		<li style="text-align: center;"><strong><%= currentUser.get('first_name') + ' ' + currentUser.get('last_name') %></strong></li>
    		<li class="divider"/>
    		<li><a id="dashboard-link"><i class="icon-th-large"/> Dashboard</a></li>
    		<li><a id="user-settings-link"><i class="icon-user"/> User Settings</a></li>
            <% if (currentUser.get('organization_manager') == true) { %>
    		<li><a id="management-link"><i class="icon-cog"/> Management</a></li>
            <% } %>
    		<li><a id="sign-out-link"><i class="icon-off"/> Sign Out</a></li>
    	</ul>
    </li>
    <li id="help-pill"><a href="#project/<%= projectId %>/help">Help</a></li>
</ul>
<div id="tab-area"></div>